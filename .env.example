# API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# Note: Never store credentials in environment variables with NEXT_PUBLIC_ prefix
# as they are exposed to the client-side. Use the login form instead.
